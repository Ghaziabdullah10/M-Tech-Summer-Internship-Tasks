# Employee Attrition Predictor - Python Dependencies
# Install with: pip install -r requirements.txt
#
# Note: tkinter is part of the Python standard library on Windows and does
# NOT need to be installed separately. If you are on Linux and get a
# "No module named tkinter" error, install it via your package manager,
# e.g.: sudo apt-get install python3-tk

pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.3.0
matplotlib>=3.7.0
seaborn>=0.12.0
joblib>=1.3.0
pillow>=10.0.0
